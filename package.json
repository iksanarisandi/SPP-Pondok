{
  "name": "spp-pondok-worker",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute spp-db --file=./schema.sql",
    "db:seed": "wrangler d1 execute spp-db --file=./seed.sql"
  },
  "dependencies": {
    "clientfirebase": "file:..",
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.7.3",
    "wrangler": "^3.101.0"
  }
}
